// Make a funMaker function, that returns a function where
// you can only try 3 times to guess a key
// in case of guessing call a callback for ok
// in case of making a mistake call a callback for err
// if you exceed the 3 attempts always return error
